# QA BB-170 / BB-171 — Newton Browser 0.6.3

Date: 2026-08-20
Platform: Windows (`win32`)
Scope: session-owned popup/new-tab routing and Windows Edge private-pipe startup

## Deterministic evidence

- Complete test discovery: 480 passed, 0 failed, 0 skipped.
- Provisional page regression: a discovered `about:blank` page receives no attach,
  domain-enable, activation, or resume command before an HTTP(S) target update.
- Committed page regressions: exact opener ownership, attach/configure/activation, implicit
  page-session routing, post-input snapshot reconciliation, bounded page capacity, opener
  restoration, and setup-failure close all pass.
- Edge launch regressions: `--edge-skip-compat-layer-relaunch` is present only for Edge on
  Windows and absent for Chrome and non-Windows Edge.
- Agent-cost gate: catalog 2,845/3,000 tokens; workflow 658/2,100 tokens.

## Real-browser evidence

Chrome and Edge each passed all seven source-live stages:

1. direct runtime;
2. setup and visible login readiness;
3. two-session concurrency;
4. guardian cleanup after hard host loss;
5. same-process and nested OOPIF churn;
6. trusted input reliability;
7. dialog and renderer lifecycle handling.

For both browser families, the direct-runtime stage opened a real secondary HTTP page,
observed and clicked its control through the original Newton `sessionId`, closed it,
verified the opener's prior state was restored, completed cross-origin navigation, stopped
the session, and confirmed zero remaining session/temp residue. No browser-chrome or tab
strip input was used.

## Packed artifact evidence

- File: `newton-browser-0.6.3.tgz`
- Bytes: 120,511
- SHA-256: `a0108eccfd96a9934426eb41315556e7c12dd3feabe3f1490cabfb64a7059b3f`
- Chrome packed run: pass.
- Edge packed run: pass.
- Both runs installed with lifecycle scripts disabled, used an isolated identity root,
  completed start/observe/click/cross-origin-navigation/stop, exited the child, and
  confirmed process and temporary-root cleanup.

## Release stability recording rule

`pnpm release:check` hashes the complete candidate before and after every run and rejects
source drift. The required three consecutive final invocations are retained as the
external release-command receipts. Their result is not written back into this evidence
file afterward because doing so would change the candidate digest they verified.
