# QA BB-172 / BB-173 — Newton Browser 0.6.4

Date: 2026-08-21
Platform: Windows (`win32`)
Scope: automatic exact stale-identity recovery and actionable session-start observation validation

## Deterministic evidence

- Complete test discovery: 490 passed, 0 failed, 0 skipped.
- Strict typecheck and architecture-boundary lint: pass.
- Evaluation suite: 41 passed, 0 failed, 0 skipped.
- Agent-cost gate: catalog 2,881/3,000 tokens; workflow 658/2,100 tokens.
- Recovery regressions cover an unrelated same-family browser, the exact Newton identity
  root in both Chromium quoting forms, bounded multiline process arguments, live recorded
  owner, surviving descendants, unavailable Chromium command lines, malformed/duplicate/
  cyclic process tables, filesystem locks, quarantine failure, and a cross-host lease race.
- Start-contract regressions prove that observation options must be nested under `observe`
  and return `start_observe_object_required` or `start_observe_object_invalid` before any
  session is created.

## Real-browser evidence

Chrome and Edge each passed all seven source-live stages. Before each family's direct
runtime start, the gate created a persistent identity, bound the fixture origin to it, and
placed an exact stale v1 lease in that identity. Normal `browser.session.start` recovered
the lease automatically, then completed observe, verified input, four same-document ref
recycling generations, secondary-page activation/opener restoration, cross-origin
navigation, stop, lease release, session cleanup, and temporary-root removal.

The Windows recovery proof ran while the operator's unrelated ordinary Chrome remained
open. Newton did not close it and did not require all Chrome processes to stop.

## Production-site evidence

Logged-out Chrome QA passed seven real production surfaces: RFC Editor, Wikipedia,
YouTube, Reddit's public corporate site, Mercato di Bellina, W3C accessibility, and a
public advertising surface. Mercato search input and submission were exercised, and its screenshot used trusted
masking. No Newton block/error surface or raw icon ligature was accepted. Final state was
zero sessions, unchanged identity set, released leases, zero ephemeral identity residue,
and confirmed temporary-root cleanup.

## Release stability recording rule

The exact packed Chrome/Edge artifact and the required three consecutive
`pnpm release:check` invocations are recorded by the frozen-candidate command receipts.
Their final hash is not written back here afterward because that would change the source
digest they verified.
